h2 = @account.linked_screen_name
h3 = @account.description

= link_to '手動更新', update_status_account_path(@account), method: :patch, class: 'btn btn-info'
= link_to '編集', edit_account_path(@account), class:'btn btn-warning'
= link_to '削除', account_path(@account), method: :delete, class: 'btn btn-danger'
= link_to '一覧に戻る', :root, class: 'btn btn-default'

.followers_count.chart
  = line_chart @followers_count_data, library: {html:'<div id="%{id}" style="width: 100%; height: 300px;">Loading...</div>'}